CREATE TABLE userfilmwatched (
    idUser INT,
    idMovie INT,
    time INT,
    totalTime INT,
    watchDate TIMESTAMP DEFAULT CURRENT_TIMESTAMP, -- Tipo di dato TIMESTAMP without time zone con valore predefinito NOW()
    PRIMARY KEY (idUser, idFilm) -- Definizione della chiave primaria composta
);